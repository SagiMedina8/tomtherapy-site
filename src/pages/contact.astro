---
import BaseLayout from "../layouts/BaseLayout.astro";
import { site, whatsappLink } from "../data/site";
---
<BaseLayout
  title="צור קשר"
  description="יצירת קשר עם SportTherapy — וואטסאפ, אימייל או טופס. תגובה תוך 24 שעות בימי פעילות."
>
  <section class="mx-auto max-w-content px-5 sm:px-8 pt-20 md:pt-28 pb-16 md:pb-24">
    <div class="grid gap-12 md:grid-cols-2 md:gap-16">
      <!-- Right col: contact details -->
      <div class="md:order-2 text-right space-y-8 fade-up">
        <div>
          <p class="eyebrow-label text-teal-deep mb-4">צור קשר</p>
          <h1 class="section-h2 mb-6">בוא נדבר.</h1>
          <p class="body-prose text-ink-soft leading-[1.75] max-w-prose">
            כל פנייה נבדקת אישית. אם אתה לא בטוח אם זה המקום בשבילך — זו בדיוק השאלה הנכונה לשאול. תכתוב לי מה קורה, ואחזיר תשובה תוך 24 שעות.
          </p>
        </div>

        <!-- Primary: WhatsApp -->
        <div class="border border-hairline bg-paper p-6 md:p-8">
          <p class="eyebrow-label text-teal-deep mb-3">מהיר</p>
          <h2 class="section-h3 font-bold mb-3">הודעה בוואטסאפ</h2>
          <p class="text-sm text-ink-soft mb-5 leading-relaxed">
            הדרך הכי מהירה להגיע אליי. תוך שעות ספורות בימי פעילות.
          </p>
          <a
            href={whatsappLink()}
            target="_blank"
            rel="noopener"
            class="btn btn-primary w-full sm:w-auto"
          >
            <span dir="ltr" class="tabular-nums">{site.whatsappDisplay}</span>
            <span aria-hidden="true">←</span>
          </a>
        </div>

        <!-- Email -->
        <div>
          <p class="eyebrow-label text-teal-deep mb-2">אימייל</p>
          <a
            href={`mailto:${site.email}`}
            class="font-serif text-[20px] text-ink hover:text-teal-deep transition-colors break-all"
          >
            {site.email}
          </a>
        </div>

        <!-- Hours -->
        <div>
          <p class="eyebrow-label text-teal-deep mb-4">שעות פעילות</p>
          <ul class="space-y-1.5 text-sm">
            {
              site.hours.map((h) => (
                <li class="flex items-center justify-between gap-4">
                  <span class="text-ink">
                    {h.day}
                    {h.label && (
                      <span class="text-ink-soft/70 text-xs"> · {h.label}</span>
                    )}
                  </span>
                  <span dir="ltr" class="text-ink-soft tabular-nums">
                    {h.hours}
                  </span>
                </li>
              ))
            }
          </ul>
        </div>

        <!-- Addresses -->
        <div class="grid sm:grid-cols-2 gap-6 pt-4 border-t border-hairline">
          {
            site.addresses.map((a) => (
              <div>
                <p class="eyebrow-label text-teal-deep mb-2">{a.title}</p>
                <p class="text-sm text-ink">
                  {a.street}, {a.city}
                  {"note" in a && a.note && (
                    <span class="block text-ink-soft/60 text-xs mt-1">
                      {a.note}
                    </span>
                  )}
                </p>
              </div>
            ))
          }
        </div>
      </div>

      <!-- Left col: form + map -->
      <div class="md:order-1 fade-up">
        <form
          name="contact"
          method="POST"
          data-netlify="true"
          netlify-honeypot="bot-field"
          class="bg-paper border border-hairline p-6 md:p-8 space-y-5"
        >
          <input type="hidden" name="form-name" value="contact" />
          <p class="hidden">
            <label>אל תמלא:<input name="bot-field" /></label>
          </p>

          <p class="eyebrow-label text-teal-deep">טופס יצירת קשר</p>
          <h2 class="section-h3 font-bold mb-2 -mt-2">שלח הודעה</h2>

          <div>
            <label for="name" class="block text-sm font-semibold text-ink mb-1.5">
              שם מלא
            </label>
            <input
              type="text"
              id="name"
              name="name"
              required
              autocomplete="name"
              class="w-full h-12 px-4 bg-cream border border-hairline focus:border-teal focus:outline-none transition-colors text-ink text-right"
            />
          </div>

          <div>
            <label for="phone" class="block text-sm font-semibold text-ink mb-1.5">
              טלפון
            </label>
            <input
              type="tel"
              id="phone"
              name="phone"
              required
              dir="ltr"
              autocomplete="tel"
              class="w-full h-12 px-4 bg-cream border border-hairline focus:border-teal focus:outline-none transition-colors text-ink text-left"
            />
          </div>

          <div>
            <label for="email" class="block text-sm font-semibold text-ink mb-1.5">
              אימייל <span class="text-ink-soft/60 text-xs font-normal">(אופציונלי)</span>
            </label>
            <input
              type="email"
              id="email"
              name="email"
              dir="ltr"
              autocomplete="email"
              class="w-full h-12 px-4 bg-cream border border-hairline focus:border-teal focus:outline-none transition-colors text-ink text-left"
            />
          </div>

          <div>
            <label for="message" class="block text-sm font-semibold text-ink mb-1.5">
              הודעה
            </label>
            <textarea
              id="message"
              name="message"
              required
              rows="5"
              class="w-full px-4 py-3 bg-cream border border-hairline focus:border-teal focus:outline-none transition-colors text-ink text-right leading-relaxed"
            ></textarea>
          </div>

          <button type="submit" class="btn btn-primary w-full">
            שלח הודעה
          </button>

          <p class="text-xs text-ink-soft/70 pt-2">
            השליחה מאובטחת ומעובדת דרך Netlify. לא נשתף את הפרטים שלך.
          </p>
        </form>

        <!-- Google Map -->
        <div class="mt-10">
          <p class="eyebrow-label text-teal-deep mb-4 text-right">מיקום</p>
          {/* TODO: second pin for Nana Home Tel Aviv once exact address is provided */}
          <div class="relative w-full overflow-hidden border border-hairline bg-paper" style="aspect-ratio: 4 / 3;">
            <iframe
              src="https://www.google.com/maps?q=%D7%90%D7%A8%D7%A0%D7%99%D7%94%206,%20%D7%A8%D7%9E%D7%AA%20%D7%92%D7%9F&output=embed"
              title="מפת מיקום — ארניה 6, רמת גן"
              loading="lazy"
              referrerpolicy="no-referrer-when-downgrade"
              class="absolute inset-0 w-full h-full border-0"
              style="min-height:320px;"
            ></iframe>
          </div>
          <p class="text-xs text-ink-soft/70 mt-3 text-right">
            הקליניקה הראשית · ארניה 6, רמת גן
          </p>
        </div>
      </div>
    </div>
  </section>
</BaseLayout>
